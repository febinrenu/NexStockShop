includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app

    level: 5

    excludePaths:
        - app/Http/Controllers/Central/SignupController.php # heavy tenant()->run() closures Larastan can't type-follow yet
